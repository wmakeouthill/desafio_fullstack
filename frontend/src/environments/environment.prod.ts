export const environment = {
    production: true,
    // Use proxy relativo (requer configuração no vercel.json)
    // ou URL direta do backend: 'https://email-classifier-api.onrender.com/api/v1'
    apiUrl: '/api/v1'
};
